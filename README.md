# Проектная работа "Веб-ларек"

Стек: HTML, SCSS, TS, Webpack

Структура проекта:
- src/ — исходные файлы проекта
- src/components/ — папка с JS компонентами
- src/components/base/ — папка с базовым кодом

Важные файлы:
- index.html — HTML-файл главной страницы
- src/types/index.ts — файл с типами
- src/main.ts — точка входа приложения
- src/scss/styles.scss — корневой файл стилей
- src/utils/constants.ts — файл с константами
- src/utils/utils.ts — файл с утилитами

## Установка и запуск
Для установки и запуска проекта необходимо выполнить команды

```
npm install
npm run start
```

или

```
yarn
yarn start
```
## Сборка

```
npm run build
```

или

```
yarn build
```
# Интернет-магазин «Web-Larёk»
«Web-Larёk» — это интернет-магазин с товарами для веб-разработчиков, где пользователи могут просматривать товары, добавлять их в корзину и оформлять заказы. Сайт предоставляет удобный интерфейс с модальными окнами для просмотра деталей товаров, управления корзиной и выбора способа оплаты, обеспечивая полный цикл покупки с отправкой заказов на сервер.

## Архитектура приложения

Код приложения разделен на слои согласно парадигме MVP (Model-View-Presenter), которая обеспечивает четкое разделение ответственности между классами слоев Model и View. Каждый слой несет свой смысл и ответственность:

Model - слой данных, отвечает за хранение и изменение данных.  
View - слой представления, отвечает за отображение данных на странице.  
Presenter - презентер содержит основную логику приложения и  отвечает за связь представления и данных.

Взаимодействие между классами обеспечивается использованием событийно-ориентированного подхода. Модели и Представления генерируют события при изменении данных или взаимодействии пользователя с приложением, а Презентер обрабатывает эти события используя методы как Моделей, так и Представлений.

### Базовый код

#### Класс Component
Является базовым классом для всех компонентов интерфейса.
Класс является дженериком и принимает в переменной `T` тип данных, которые могут быть переданы в метод `render` для отображения.

Конструктор:  
`constructor(container: HTMLElement)` - принимает ссылку на DOM элемент за отображение, которого он отвечает.

Поля класса:  
`container: HTMLElement` - поле для хранения корневого DOM элемента компонента.

Методы класса:  
`render(data?: Partial<T>): HTMLElement` - Главный метод класса. Он принимает данные, которые необходимо отобразить в интерфейсе, записывает эти данные в поля класса и возвращает ссылку на DOM-элемент. Предполагается, что в классах, которые будут наследоваться от `Component` будут реализованы сеттеры для полей с данными, которые будут вызываться в момент вызова `render` и записывать данные в необходимые DOM элементы.  
`setImage(element: HTMLImageElement, src: string, alt?: string): void` - утилитарный метод для модификации DOM-элементов `<img>`


#### Класс Api
Содержит в себе базовую логику отправки запросов.

Конструктор:  
`constructor(baseUrl: string, options: RequestInit = {})` - В конструктор передается базовый адрес сервера и опциональный объект с заголовками запросов.

Поля класса:  
`baseUrl: string` - базовый адрес сервера  
`options: RequestInit` - объект с заголовками, которые будут использованы для запросов.

Методы:  
`get(uri: string): Promise<object>` - выполняет GET запрос на переданный в параметрах ендпоинт и возвращает промис с объектом, которым ответил сервер  
`post(uri: string, data: object, method: ApiPostMethods = 'POST'): Promise<object>` - принимает объект с данными, которые будут переданы в JSON в теле запроса, и отправляет эти данные на ендпоинт переданный как параметр при вызове метода. По умолчанию выполняется `POST` запрос, но метод запроса может быть переопределен заданием третьего параметра при вызове.  
`handleResponse(response: Response): Promise<object>` - защищенный метод проверяющий ответ сервера на корректность и возвращающий объект с данными полученный от сервера или отклоненный промис, в случае некорректных данных.

#### Класс EventEmitter
Брокер событий реализует паттерн "Наблюдатель", позволяющий отправлять события и подписываться на события, происходящие в системе. Класс используется для связи слоя данных и представления.

Конструктор класса не принимает параметров.

Поля класса:  
`_events: Map<string | RegExp, Set<Function>>)` -  хранит коллекцию подписок на события. Ключи коллекции - названия событий или регулярное выражение, значения - коллекция функций обработчиков, которые будут вызваны при срабатывании события.

Методы класса:  
`on<T extends object>(event: EventName, callback: (data: T) => void): void` - подписка на событие, принимает название события и функцию обработчик.  
`emit<T extends object>(event: string, data?: T): void` - инициализация события. При вызове события в метод передается название события и объект с данными, который будет использован как аргумент для вызова обработчика.  
`trigger<T extends object>(event: string, context?: Partial<T>): (data: T) => void` - возвращает функцию, при вызове которой инициализируется требуемое в параметрах событие с передачей в него данных из второго параметра.

### Данные

#### Интерфейс IProduct 
Описывает структуру товара, который доступен в интернет-магазине.

`id: string` — идентификатор товара.
`description:` string — описание товара.
`image: string` — ссылка на изображение товара.
`title: string` — название товара.
`category: string` — категория товара.
`price: number | null` — цена товара. Может быть null, если товар недоступен.


#### Интерфейс IBuyer
Описывает данные покупателя, необходимые для оформления заказа.

`payment: TPayment` — выбранный способ оплаты ('card' или 'cash').
`email: string` — электронная почта покупателя.
`phone: string` — номер телефона покупателя.
`address: string` — адрес доставки заказа.

### Модели данных

#### класс Products
Хранение товаров, которые можно купить в приложении.

Конструктор класса не принимает параметров.

Поля класса: 
`items: IProduct[]` - хранит массив всех товаров.
`selected: IProduct | null` хранит товар, выбранный для подробного отображения.

Методы класса:
`setItems(items: IProduct[]): void` - сохранения массива товаров полученного в параметрах метода.
`getItems(): IProduct[]` - получение массива товаров из модели.
`getById(id: string): IProduct | undefined` - получение одного товара по его id.
`setSelected(product: IProduct): void` - сохранения товара для подробного отображения.
`getSelected(): IProduct | null` - получение товара для подробного отображения.

#### класс Cart
хранение товаров, которые пользователь выбрал для покупки.

Конструктор класса не принимает параметров.

Поля класса: 
`items: IProduct[]` - хранит массив товаров, выбранных покупателем для покупки.

Методы класса:
`getItems(): IProduct[]` - получение массива товаров, которые находятся в корзине.
`addItem(product: IProduct): void` - добавление товара, который был получен в параметре в массив корзины.
`removeItem(product: IProduct): void` - удаление товара, полученного в параметре из массива корзины.
`clear(): void` - очистка корзины.
`getTotal(): number` - получение стоимости всех товаров в корзине.
`getCount(): number` - получение количества товаров в корзине.
`hasItem(id: string): boolean` - проверка наличия товара в корзине по его id, полученного в параметр метода.

#### класс Buyer

Конструктор класса не принимает параметров.

Поля класса: 
`payment: TPayment` - вид оплаты.
`email: string` - email.  
`phone: string` - телефон.
`address: string` - адресс.

Методы класса:
`setData(data: IBuyer): void` - сохранение данных в модели. Один общий метод или отдельные методы для каждого поляю
`getData(): IBuyer | null` - получение всех данных покупателя.
`clear(): void` - очистка данных покупателя.
`validate(): boolean` - валидация данных.

### Слой коммуникации

#### класс LarekApi
Класс твечает за взаимодействие с сервером.

Конструктор:
`constructor(api: IApi)` — принимает экземпляр класса Api.

Методы:
`getProducts(): Promise<IProduct[]>` — выполняет GET-запрос на `/product/` и возвращает массив товаров.
`createOrder(order: IOrder): Promise<IOrderResult>` — выполняет POST-запрос на `/order/` и отправляет данные заказа. Возвращает объект с `id` заказа и `total` суммой.

### Слой представления

#### класс Header
Отвечает за отоброжение шапки сайта и счетчика товаров в корзине.


Поля класса:
`basketButton: HTMLButtonElement;` - кнопка открытия корзины.
`counterElement: HTMLElement;` - Элемент для отображения счетчика товаров в корзине.

Конструктор:
`events: IEvents, container: HTMLElement` - принимает брокер событий для коммуникации между компонентами и DOM-элемент для рендеринга.

Методы:
`set counter(value: number)` - обновляет значения счетчика товаров в корзине.

#### класс Gallery
Отображает каталог товаров.

Конструктор:
`container: HTMLElement` - Dom-элемент для рендера.

Методы:
`set catalog(items: HTMLElement[])` - отвечает за отображения каталога товаров.

#### класс Modal
Отоброжает и управляет модальными окнами.

Поля класса:
`content: HTMLElement` - контейнер модального окна.
`button: HTMLButtonElement`- кнопка закрытия модального окна.

Конструктор:
`events: IEvents, container: HTMLElement` - принимает брокер событий для коммуникации между компонентами и DOM-элемент для рендеринга.

Методы:
`contentSet(item: HTMLElement)` - Заменяет содержимое контейнера на переданный HTML-элемент.
`open(): void` - Открывает модальное окно и активирует обработчик клавиши Escape.
`close(): void` - Закрывает модальное окно и деактивирует обработчик клавиши Escape.


#### класс Success
Отображает окно успешного завершения заказа с информацией о списании средств.

Поля класса:
`elementTotal: HTMLElement` - элемент для отображения суммы списания.
`button: HTMLButtonElement` - кнопка закрытия окна успешного заказа.

Конструктор:
`events: IEvents, container: HTMLElement` - принимает брокер событий для коммуникации между компонентами и DOM-элемент для рендеринга.

Методы:
`set total(value: number)` - устанавливает сумму списания в формате "Списано X синапсов".

#### класс Card
Базовый абстрактный класс для отоброжения карточек товаров.

Поля класса:
`productTitle: HTMLElement` - DOM-элемент для отображения названия товара.
`productPrice: HTMLElement` - DOM-элемент для отображения цены товара.
`productId: string` - внутреннее хранение идентификатора товара.

Конструктор:
`container: HTMLElement` - DOM-элемент для рендеринга.

Методы:
`set title(value: string)` - устанавливает название карточки.

`set price(value: number | null)` - форматирует и устанавливает цену ("X синапсов" или "Бесценно").

`set id(value: string)` - сохраняет идентификатор товара.

#### класс CardCatalog
Отображает карточку товара в каталоге. Наследует функциональность базовой карточки и добавляет изображение товара и категорию.

Поля класса:
`productImage: HTMLImageElement` - DOM-элемент для отображения изображения товара.
`productCategory: HTMLElement` - DOM-элемент для отображения категории товара.

Конструктор:
`container: HTMLElement, actions?: ICardActions` - принимает корневой DOM-элемент карточки и опциональный объект действий

Методы:
`set category(value: string)` - устанавливает категорию товара и применяет соответствующий CSS-класс. Проходит по всем категориям в categoryMap и добавляет/удаляет CSS-классы в зависимости от соответствия.

`set image(value: string)` - устанавливает изображение товара используя унаследованный метод setImage. В качестве alt-текста использует заголовок карточки.

#### класс CardBasket
Отображает карточку товара в корзине. Наследует функциональность базовой карточки и добавляет счетчик количества товара и кнопку удаления.


Поля класса:
`productCounter: HTMLElement` - DOM-элемент для отображения количества товара в корзине.
`deleteProductButton: HTMLButtonElement` - кнопка удаления товара из корзины.

Конструктор:
`events: IEvents, container: HTMLElement` - принимает брокер событий для коммуникации между компонентами и DOM-элемент для рендеринга.

Методы:
`set counter(value: number)` - устанавливает количество товара в корзине.

#### класс cardPreview
Отображает карточку товара в превью. Наследует функциональность базовой карточки и добавляет описание товара, изображение, категорию и кнопку добавления/удаления из корзины.

Поля класса:
`productCategory: HTMLElement` - DOM-элемент для отображения категории товара.

`productImage: HTMLImageElement` - DOM-элемент для отображения изображения товара.

`productDescription: HTMLElement` - DOM-элемент для отображения описания товара.

`productInBasket: boolean` - состояние, указывающее находится ли товар в корзине.

`productButton: HTMLButtonElement` - кнопка для добавления/удаления товара из корзины.


Конструктор:
`events: IEvents` - брокер событий для коммуникации между компонентами.
`container: HTMLElement` - DOM-элемент для рендеринга.

Методы:
`set descripton(value: string)` - устанавливает описание товара.

`set category(value: string)` - устанавливает категорию товара и применяет соответствующий CSS-класс. Проходит по всем категориям в categoryMap и добавляет/удаляет CSS-классы в зависимости от соответствия.

`set image(value: string)` - устанавливает изображение товара используя унаследованный метод setImage.

`set inBasket(value: boolean)` - устанавливает состояние товара в корзине и меняет текст кнопки соответственно ("Удалить из корзины" или "Купить").

`set price(value: number | null)` - устанавливает цену товара. Если цена равна null, делает кнопку недоступной и меняет её текст на "Недоступно".

#### класс Basket
Отображает корзину товаров с списком элементов, общей стоимостью и кнопкой оформления заказа.

Поля класса:
`basketList: HTMLElement` - DOM-элемент для отображения списка товаров в корзине

`basketPrice: HTMLElement` - DOM-элемент для отображения общей стоимости товаров

`basketButton: HTMLButtonElement` - кнопка для перехода к оформлению заказа

Конструктор:
`container: HTMLElement` - DOM-элемент для рендеринга корзины

`events: IEvents` - брокер событий для коммуникации между компонентами

Методы:
`set elementsList(list: HTMLElement[])` - устанавливает список элементов корзины. Если список пуст, отображает сообщение "Корзина пуста" и блокирует кнопку оформления заказа. При наличии элементов очищает список и добавляет новые элементы, активируя кнопку оформления.

`set totalPrice(price: number)` - устанавливает общую стоимость товаров в корзине в формате "X синапсов".

#### класс Form
Абстрактный базовый класс для форм, обеспечивающий базовую функциональность валидации и отправки данных.


Поля класса:
`submitButton: HTMLButtonElement` - кнопка отправки формы

`errorsElement: HTMLElement` - элемент для отображения ошибок валидации

Конструктор:
`events: IEvents` - брокер событий для коммуникации между компонентами

`container: HTMLElement` - DOM-элемент для рендеринга формы

Методы:
`protected handleSubmit(event: Event)` - обработчик события отправки формы. Отменяет стандартное поведение формы и вызывает отправку только при успешной валидации

`protected abstract isValid(): boolean` - абстрактный метод проверки валидности формы (должен быть реализован в дочерних классах)

`protected abstract submit(): void` - абстрактный метод отправки формы (должен быть реализован в дочерних классах)

`protected updateSubmitButton()` - обновляет состояние кнопки отправки на основе результата валидации

`protected showErrors(errors: string[])` - отображает ошибки валидации в элементе ошибок

#### класс FormOrder
Отображает форму оформления заказа с выбором способа оплаты и вводом адреса доставки. Наследует функциональность базовой формы.


Поля класса:
`cardButton: HTMLButtonElement` - кнопка выбора оплаты картой

`cashButton: HTMLButtonElement` - кнопка выбора оплаты наличными

`addressInput: HTMLInputElement` - поле ввода адреса доставки

`selectedPayment: string` - выбранный способ оплаты

Конструктор:
`events: IEvents` - брокер событий для коммуникации между компонентами

`container: HTMLElement` - DOM-элемент для рендеринга формы

Методы:
`init()` - инициализирует обработчики событий для кнопок выбора оплаты и поля ввода адреса

`protected updateSubmitButton(): void` - обновляет визуальное состояние кнопок оплаты, добавляя/удаляя активный стиль в зависимости от выбранного способа оплаты

`protected isValid(): boolean` - проверяет валидность формы (выбран способ оплаты и указан адрес)

`protected submit(): void` - отправляет данные заказа, генерируя событие 'order:next' с адресом и способом оплаты

`protected validate(): void` - выполняет валидацию формы, отображает ошибки и обновляет состояние кнопки отправки

#### класс FormContacts
Отображает форму ввода контактных данных покупателя для оформления заказа. Наследует функциональность базовой формы

Поля класса:
`phoneInput: HTMLInputElement` - поле ввода номера телефона

`emailInput: HTMLInputElement` - поле ввода электронной почты

Конструктор:
`events: IEvents` - брокер событий для коммуникации между компонентами

`container: HTMLElement` - DOM-элемент для рендеринга формы

Методы:
`init()` - инициализирует обработчики событий для полей ввода телефона и email

`protected isValid(): boolean` - проверяет валидность формы (заполнены оба поля: телефон и email)

`protected submit(): void` - отправляет контактные данные, генерируя событие 'order:submit' с номером телефона и email

`protected validate(): void` - выполняет валидацию формы, отображает ошибки и обновляет состояние кнопки отправки

#### класс 



Поля класса:


Конструктор:


Методы:
